#include <bits/stdc++.h>

using namespace std;

typedef long long ll;



int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);
    int n, m;
    cin >> n >> m;
    cout << string(n + 1, '1') << "\n" << string(n, '8') + "9" << "\n";
}